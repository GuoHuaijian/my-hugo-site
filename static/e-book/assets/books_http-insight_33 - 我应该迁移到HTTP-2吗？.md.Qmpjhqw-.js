import{_ as p,o as a,c as e,ae as t}from"./chunks/framework.Iv6F95cJ.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"books/http-insight/33 - 我应该迁移到HTTP-2吗？.md","filePath":"books/http-insight/33 - 我应该迁移到HTTP-2吗？.md"}'),s={name:"books/http-insight/33 - 我应该迁移到HTTP-2吗？.md"};function n(P,T,i,l,r,H){return a(),e("div",null,[...T[0]||(T[0]=[t(`<p>这一讲是“飞翔篇”的最后一讲，而HTTP的所有知识也差不多快学完了。</p><p>前面你已经看到了新的HTTP/2和HTTP/3协议，了解了它们的特点和工作原理，如果再联系上前几天“安全篇”的HTTPS，你可能又会发出疑问：</p><p>“刚费了好大的力气升级到HTTPS，这又出了一个HTTP/2，还有再次升级的必要吗？”</p><p>与各大浏览器“强推”HTTPS的待遇不一样，HTTP/2的公布可谓是“波澜不惊”。虽然它是HTTP协议的一个重大升级，但Apple、Google等科技巨头并没有像HTTPS那样给予大量资源的支持。</p><p>直到今天，HTTP/2在互联网上还是处于“不温不火”的状态，虽然已经有了不少的网站改造升级到了HTTP/2，但普及的速度远不及HTTPS。</p><p>所以，你有这样的疑问也是很自然的，升级到HTTP/2究竟能给我们带来多少好处呢？到底“值不值”呢？</p><h2 id="http-2的优点" tabindex="-1">HTTP/2的优点 <a class="header-anchor" href="#http-2的优点" aria-label="Permalink to &quot;HTTP/2的优点&quot;">&amp;ZeroWidthSpace;</a></h2><p>前面的几讲主要关注了HTTP/2的内部实现，今天我们就来看看它有哪些优点和缺点。</p><p>首先要说的是，HTTP/2最大的一个优点是<strong>完全保持了与HTTP/1的兼容</strong>，在语义上没有任何变化，之前在HTTP上的所有投入都不会浪费。</p><p>因为兼容HTTP/1，所以HTTP/2也具有HTTP/1的所有优点，并且“基本”解决了HTTP/1的所有缺点，安全与性能兼顾，可以认为是“更安全的HTTP、更快的HTTPS”。</p><p>在安全上，HTTP/2对HTTPS在各方面都做了强化。下层的TLS至少是1.2，而且只能使用前向安全的密码套件（即ECDHE），这同时也就默认实现了“TLS False Start”，支持1-RTT握手，所以不需要再加额外的配置就可以自动实现HTTPS加速。</p><p>安全有了保障，再来看HTTP/2在性能方面的改进。</p><p>你应该知道，影响网络速度的两个关键因素是“<strong>带宽</strong>”和“<strong>延迟</strong>”，HTTP/2的头部压缩、多路复用、流优先级、服务器推送等手段其实都是针对这两个要点。</p><p>所谓的“带宽”就是网络的传输速度。从最早的56K/s，到如今的100M/s，虽然网速已经是“今非昔比”，比从前快了几十倍、几百倍，但仍然是“稀缺资源”，图片、视频这样的多媒体数据很容易会把带宽用尽。</p><p>节约带宽的基本手段就是压缩，在HTTP/1里只能压缩body，而HTTP/2则可以用HPACK算法压缩header，这对高流量的网站非常有价值，有数据表明能节省大概5%~10%的流量，这是实实在在的“真金白银”。</p><p>与HTTP/1“并发多个连接”不同，HTTP/2的“多路复用”特性要求对<strong>一个域名（或者IP）只用一个TCP连接</strong>，所有的数据都在这一个连接上传输，这样不仅节约了客户端、服务器和网络的资源，还可以把带宽跑满，让TCP充分“吃饱”。</p><p>这是为什么呢？</p><p>我们来看一下在HTTP/1里的长连接，虽然是双向通信，但任意一个时间点实际上还是单向的：上行请求时下行空闲，下行响应时上行空闲，再加上“队头阻塞”，实际的带宽打了个“对折”还不止（可参考<a href="https://time.geekbang.org/column/article/104949" target="_blank" rel="noreferrer">第17讲</a>）。</p><p>而在HTTP/2里，“多路复用”则让TCP开足了马力，“全速狂奔”，多个请求响应并发，每时每刻上下行方向上都有流在传输数据，没有空闲的时候，带宽的利用率能够接近100%。所以，HTTP/2只使用一个连接，就能抵得过HTTP/1里的五六个连接。</p><p>不过流也可能会有依赖关系，可能会存在等待导致的阻塞，这就是“延迟”，所以HTTP/2的其他特性就派上了用场。</p><p>“优先级”可以让客户端告诉服务器，哪个文件更重要，更需要优先传输，服务器就可以调高流的优先级，合理地分配有限的带宽资源，让高优先级的HTML、图片更快地到达客户端，尽早加载显示。</p><p>“服务器推送”也是降低延迟的有效手段，它不需要客户端预先请求，服务器直接就发给客户端，这就省去了客户端解析HTML再请求的时间。</p><h2 id="http-2的缺点" tabindex="-1">HTTP/2的缺点 <a class="header-anchor" href="#http-2的缺点" aria-label="Permalink to &quot;HTTP/2的缺点&quot;">&amp;ZeroWidthSpace;</a></h2><p>说了一大堆HTTP/2的优点，再来看看它有什么缺点吧。</p><p>听过上一讲HTTP/3的介绍，你就知道HTTP/2在TCP级别还是存在“队头阻塞”的问题。所以，如果网络连接质量差，发生丢包，那么TCP会等待重传，传输速度就会降低。</p><p>另外，在移动网络中发生IP地址切换的时候，下层的TCP必须重新建连，要再次“握手”，经历“慢启动”，而且之前连接里积累的HPACK字典也都消失了，必须重头开始计算，导致带宽浪费和时延。</p><p>刚才也说了，HTTP/2对一个域名只开一个连接，所以一旦这个连接出问题，那么整个网站的体验也就变差了。</p><p>而这些情况下HTTP/1反而不会受到影响，因为它“本来就慢”，而且还会对一个域名开6~8个连接，顶多其中的一两个连接会“更慢”，其他的连接不会受到影响。</p><h2 id="应该迁移到http-2吗" tabindex="-1">应该迁移到HTTP/2吗？ <a class="header-anchor" href="#应该迁移到http-2吗" aria-label="Permalink to &quot;应该迁移到HTTP/2吗？&quot;">&amp;ZeroWidthSpace;</a></h2><p>说到这里，你对迁移到HTTP/2是否已经有了自己的判断呢？</p><p>在我看来，HTTP/2处于一个略“尴尬”的位置，前面有“老前辈”HTTP/1，后面有“新来者”HTTP/3，即有“老前辈”的“打压”，又有“新来者”的“追赶”，也就难怪没有获得市场的大力“吹捧”了。</p><p>但这绝不是说HTTP/2“一无是处”，实际上HTTP/2的性能改进效果是非常明显的，Top 1000的网站中已经有超过40%运行在了HTTP/2上，包括知名的Apple、Facebook、Google、Twitter等等。仅用了四年的时间，HTTP/2就拥有了这么大的市场份额和巨头的认可，足以证明它的价值。</p><p>因为HTTP/2的侧重点是“性能”，所以“是否迁移”就需要在这方面进行评估。如果网站的流量很大，那么HTTP/2就可以带来可观的收益；反之，如果网站流量比较小，那么升级到HTTP/2就没有太多必要了，只要利用现有的HTTP再优化就足矣。</p><p>不过如果你是新建网站，我觉得完全可以跳过HTTP/1、HTTPS，直接“一步到位”，上HTTP/2，这样不仅可以获得性能提升，还免去了老旧的“历史包袱”，日后也不会再有迁移的烦恼。</p><p>顺便再多嘴一句，HTTP/2毕竟是“下一代”HTTP协议，它的很多特性也延续到了HTTP/3，提早升级到HTTP/2还可以让你在HTTP/3到来时有更多的技术积累和储备，不至于落后于时代。</p><h2 id="配置http-2" tabindex="-1">配置HTTP/2 <a class="header-anchor" href="#配置http-2" aria-label="Permalink to &quot;配置HTTP/2&quot;">&amp;ZeroWidthSpace;</a></h2><p>假设你已经决定要使用HTTP/2，应该如何搭建服务呢？</p><p>因为HTTP/2“事实上”是加密的，所以如果你已经在“安全篇”里成功迁移到了HTTPS，那么在Nginx里启用HTTP/2简直可以说是“不费吹灰之力”，只需要在server配置里再多加一个参数就可以搞定了。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>server {</span></span>
<span class="line"><span>    listen       443 ssl http2;</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>    server_name  www.xxx.net;</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>    ssl_certificate         xxx.crt;</span></span>
<span class="line"><span>    ssl_certificate_key     xxx.key;</span></span></code></pre></div><p>注意“listen”指令，在“ssl”后面多了一个“http2”，这就表示在443端口上开启了SSL加密，然后再启用HTTP/2。</p><p>配置服务器推送特性可以使用指令“http2_push”和“http2_push_preload”：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http2_push         /style/xxx.css;</span></span>
<span class="line"><span>http2_push_preload on;</span></span></code></pre></div><p>不过如何合理地配置推送是个难题，如果推送给浏览器不需要的资源，反而浪费了带宽。</p><p>这方面暂时没有一般性的原则指导，你必须根据自己网站的实际情况去“猜测”客户端最需要的数据。</p><p>优化方面，HTTPS的一些策略依然适用，比如精简密码套件、ECC证书、会话复用、HSTS减少重定向跳转等等。</p><p>但还有一些优化手段在HTTP/2里是不适用的，而且还会有反效果，比如说常见的精灵图（Spriting）、资源内联（inlining）、域名分片（Sharding）等，至于原因是什么，我把它留给你自己去思考（提示，与缓存有关）。</p><p>还要注意一点，HTTP/2默认启用header压缩（HPACK），但并没有默认启用body压缩，所以不要忘了在Nginx配置文件里加上“gzip”指令，压缩HTML、JS等文本数据。</p><h2 id="应用层协议协商-alpn" tabindex="-1">应用层协议协商（ALPN） <a class="header-anchor" href="#应用层协议协商-alpn" aria-label="Permalink to &quot;应用层协议协商（ALPN）&quot;">&amp;ZeroWidthSpace;</a></h2><p>最后说一下HTTP/2的“服务发现”吧。</p><p>你有没有想过，在URI里用的都是HTTPS协议名，没有版本标记，浏览器怎么知道服务器支持HTTP/2呢？为什么上来就能用HTTP/2，而不是用HTTP/1通信呢？</p><p>答案在TLS的扩展里，有一个叫“<strong>ALPN</strong>”（Application Layer Protocol Negotiation）的东西，用来与服务器就TLS上跑的应用协议进行“协商”。</p><p>客户端在发起“Client Hello”握手的时候，后面会带上一个“ALPN”扩展，里面按照优先顺序列出客户端支持的应用协议。</p><p>就像下图这样，最优先的是“h2”，其次是“http/1.1”，以前还有“spdy”，以后还可能会有“h3”。</p><p><img src="https://static001.geekbang.org/resource/image/d8/b0/d8f8606948bbd63c31466e464c1956b0.png?wh=1233%2A545" alt="" loading="lazy" referrerpolicy="no-referrer"></p><p>服务器看到ALPN扩展以后就可以从列表里选择一种应用协议，在“Server Hello”里也带上“ALPN”扩展，告诉客户端服务器决定使用的是哪一种。因为我们在Nginx配置里使用了HTTP/2协议，所以在这里它选择的就是“h2”。</p><p><img src="https://static001.geekbang.org/resource/image/19/a7/19be1138574589458c96040e1a23b3a7.png?wh=1197%2A361" alt="" loading="lazy" referrerpolicy="no-referrer"></p><p>这样在TLS握手结束后，客户端和服务器就通过“ALPN”完成了应用层的协议协商，后面就可以使用HTTP/2通信了。</p><h2 id="小结" tabindex="-1">小结 <a class="header-anchor" href="#小结" aria-label="Permalink to &quot;小结&quot;">&amp;ZeroWidthSpace;</a></h2><p>今天我们讨论了是否应该迁移到HTTP/2，还有应该如何迁移到HTTP/2。</p><ol><li>HTTP/2完全兼容HTTP/1，是“更安全的HTTP、更快的HTTPS”，头部压缩、多路复用等技术可以充分利用带宽，降低延迟，从而大幅度提高上网体验；</li><li>TCP协议存在“队头阻塞”，所以HTTP/2在弱网或者移动网络下的性能表现会不如HTTP/1；</li><li>迁移到HTTP/2肯定会有性能提升，但高流量网站效果会更显著；</li><li>如果已经升级到了HTTPS，那么再升级到HTTP/2会很简单；</li><li>TLS协议提供“ALPN”扩展，让客户端和服务器协商使用的应用层协议，“发现”HTTP/2服务。</li></ol><h2 id="课下作业" tabindex="-1">课下作业 <a class="header-anchor" href="#课下作业" aria-label="Permalink to &quot;课下作业&quot;">&amp;ZeroWidthSpace;</a></h2><ol><li>和“安全篇”的第29讲类似，结合自己的实际情况，分析一下是否应该迁移到HTTP/2，有没有难点？</li><li>精灵图（Spriting）、资源内联（inlining）、域名分片（Sharding）这些手段为什么会对HTTP/2的性能优化造成反效果呢？</li></ol><p>欢迎你把自己的学习体会写在留言区，与我和其他同学一起讨论。如果你觉得有所收获，也欢迎把文章分享给你的朋友。</p><p><img src="https://static001.geekbang.org/resource/image/fb/55/fb986a7575ec902c86c17a937dbca655.png?wh=1769%2A3444" alt="unpreview" loading="lazy" referrerpolicy="no-referrer"> 精选留言（15） 我行我素 👍（58） 💬（2）2.因为HTTP/2中使用小颗粒化的资源，优化了缓存，而使用精灵图就相当于传输大文件，但是大文件会延迟客户端的处理执行，并且缓存失效的开销很昂贵，很少数量的数据更新就会使整个精灵图失效，需要重新下载(http1中使用精灵图是为了减少请求)； HTTP1中使用内联资源也是为了减少请求，内联资源没有办法独立缓存，破坏了HTTP/2的多路复用和优先级策略； 域名分片在HTTP1中是为了突破浏览器每个域名下同时连接数，但是这在HTTP/2中使用多路复用解决了这个问题，如果使用域名分片反而会限制HTTP2的自由发挥2019-08-12前端西瓜哥 👍（18） 💬（2）课下作业的第二题的个人理解。</p><p>问： 精灵图（Spriting）、资源内联（inlining）、域名分片（Sharding）这些手段为什么会对 HTTP/2 的性能优化造成反效果呢？</p><p>答：主要是缓存和请求速度的原因。使用 HTTP/2 后，请求就可以做到乱序收发、多路复用。</p><ol><li><p>图片就算很多，在 HTTP/2 也可以做到 “并发”。使用了精灵图的话，首先文件变大了，在 HTTP/2 中相比分开请求要更慢，而且不利于缓存（比如修改了其中几个图片）。</p></li><li><p>资源内联，是指将一个资源作为另一个资源的一部分，使二者作为一个整体的资源来请求，比如 HTML 文件里嵌入 base64 的图片，该方案是为了减少 HTTP/1 下的请求数，加快网页响应时间。HTTP/2 不存在网页加载变慢的情况，而且不内联的话，能更好地发挥缓存的优势（比如图片是固定的，但 HTML 是动态的）。</p></li><li><p>域名分片。这个不是很懂，老师在本文也说到： “HTTP/2 对一个域名只开一个连接，所以一旦这个连接出问题，那么整个网站的体验也就变差了”。这么来说，域名分片建立多个连接，貌似就可以解决这个问题？如果不考虑这个问题的话，因为多路复用的原因，就算多了一个连接，也只是变成了两个多路复用，并没有提高多少效率，倒不是很有必要，而且代码实现也会比较麻烦。2019-08-13Geek_hfbm3l 👍（10） 💬（1）老师这么理解有错吗？ http1 最好把多个请求合成一个请求（比如精灵图，资源内联，域名分片等），原因是 http1 存在 http 的队头阻塞，每次发送新的请求都又需要带上没有压缩的请求头，DNS及时有缓存也需要查找，而且有时候会被清空。http2 某些情况比如精灵图，资源内联就不需要合成一个请求，因为 http2 是基于流和帧的，没有了 http1 的队头阻塞，可以并发多个请求，而且 http2 的请求头使用了 hpage 压缩算法，下一次请求时请求头的长度会非常短（比如 65 一个数字就能表示以前的一长串 UA）。而且还加入了服务器推送，服务器会预先把可能需要的资源先推送给你。如果还是使用一个请求，可能会因为只更新一点点资源而更新整个缓存（比如更新精灵图或资源内联其中的一小部分）。2019-08-13许童童 👍（6） 💬（1）分析一下是否应该迁移到 HTTP/2，有没有难点？ 从慢、贵、难三个角度来分析 速度快，免费，部署简单，具体分析过程就不写了，我觉得应该立即迁移到 HTTP/2。</p></li></ol><p>精灵图（Spriting）、资源内联（inlining）、域名分片（Sharding）这些手段为什么会对 HTTP/2 的性能优化造成反效果呢？ 精灵图、资源内联可以减少HTTP请求数但加大单个请求的大小、域名分片为了突破与同一域名最多建立6个连接的限制，HTTP/2使用流并发请求响应多个资源，完全不需要此优化，相反还会多建立TCP连接浪费资源。 2019-08-12阿锋 👍（3） 💬（1）HTTP/1 里实现了长连接，为啥还会对一个域名开 6~8 个连接，是不是http为了解决http自身的对头阻塞，不要让http等待完响应之后，才发出下一个请求，而打开多个连接？这些打开了的连接会得到重复利用，就是一段时间内不会关闭？这样才会比HTTP（短连接）高效？ 可以解析一下内联资源，域名分片，什么意思？2019-08-12看，有只猪 👍（2） 💬（1）老师，我还有一个问题，既然通过ALPN协商了通信采用的协议，那建立好TLS连接后，后续操作直接采用HTTP2即可，为什么还需要发送Magic呢2019-10-28ifelse 👍（1） 💬（1）看了下tmall.com，协议写的是https2023-02-04球魁 👍（1） 💬（1）老师你好，目前我们团队正在尝试升级HTTP/2协议，考虑到原域名下有很多其它团队的接口服务，我们使用一个新的域名来升级HTTP/2，然后把接口逐步迁移过去。最近打算迁移一个流量很大的接口大概2000qps，前期对其做了5%小流量实验，但是结果很不理想，性能并没有提升反而劣化了。跑了一下每个阶段的耗时，发现服务端耗时大概有15-20ms的下降（接口平均耗时900ms），tcp有10ms的下降，ssl也有5-10ms的下降，请问有什么好的优化升级建议嘛？2022-03-22明月 👍（1） 💬（2）精灵图、资源内联在http2效果不好 终极原因也还是http2只能建立一个连接 这样一个大文件传输会占用这唯一的连接 缓存失效后又要更新占用这个连接更新资源 我理解的对吗2020-04-08看，有只猪 👍（1） 💬（1）老师，请问我对服务发现的理解对吗？ 针对加密版本的HTTP/2，客户端需要<code>Client Hello</code>的<code>ALPN</code>中添加支持的协议，由服务器选择，服务器也通过<code>ALPN</code>告知客户端后续通信采用的协议。 非加密版本的HTTP/1中，客户端建立TCP连接后，将发送Magic。服务器识别到后，后续通信采用HTTP/2。2019-10-27Luke 👍（1） 💬（2）TCP 协议存在“队头阻塞”，所以 HTTP/2 在弱网或者移动网络下的性能表现会不如 HTTP/1； 老师，HTTP/1，HTTP/2都是基于TCP协议的，在同样“队头阻塞”的情况下，为什么HTTP/1的性能要优于HTTP/2 ？2019-09-03👻 小二 👍（1） 💬（1）有一点我不明白， http2只有一条连接， 那在多线程并发时， 应该需要锁吧， 也就是同一瞬间只能一个写入，多几条连接， 不是能并发吗？ 还是说网卡层同一瞬间也只能处理一个写的请求， 所以并不并发都没关系。不过在tcp层面上的队头阻塞还是有关系的吧， 多 开一个连接， 就不会都阻塞住了</p><p>如果有多个网卡呢？2019-08-23路边的猪 👍（0） 💬（1）为什么在使用http2的时候不可以在一个网站内多开几个tcp连接，这岂不是更高效2023-02-13Geek_0e3b40 👍（0） 💬（1）老师，请教一个关于http/2的问题： 如果客户端向服务端同时发起多个请求，那服务端收到请求的顺序会和客户端发起的顺序一样吗？2022-12-09Geek_8476da 👍（0） 💬（1）学习http到最好 好的方法应该是实践！！！2022-09-02</p>`,69)])])}const c=p(s,[["render",n]]);export{h as __pageData,c as default};
